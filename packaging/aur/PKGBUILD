# Maintainer: stektus <stektus@users.noreply.github.com>
# AUR package "monti-bin": repackages the .deb from the GitHub release.

pkgname=monti-bin
pkgver=0.9.3
pkgrel=1
pkgdesc="Mount your clouds — a friendly GUI for rclone (Google Drive, Dropbox, WebDAV, S3, SFTP as local folders)"
arch=('x86_64')
url="https://github.com/stektus/monti"
license=('MIT')
# libayatana-appindicator is a dependency, not an option: without it there is
# no tray icon at all, and "install this library, then you get a tray" is
# exactly the kind of homework this app exists to spare people. The .deb and
# .rpm require it for the same reason; it is under a megabyte.
depends=('webkit2gtk-4.1' 'gtk3' 'fuse3' 'libayatana-appindicator')
optdepends=(
  'rclone: use the system engine instead of the one Monti downloads itself'
)
provides=('monti')
conflicts=('monti')
options=('!strip' '!debug')
source=("Monti-$pkgver.deb::$url/releases/download/v$pkgver/Monti_${pkgver}_amd64.deb"
        "LICENSE-$pkgver::$url/raw/v$pkgver/LICENSE")
# Refreshed with `updpkgsums` after each release — the files do not exist
# until the release workflow has published them.
sha256sums=('6f25a75f0418965220d0fccf3a11d120f25fa6c225fbab3bb6a65d03140c8d5c'
            '16fbf90b022ad1dbc88cbcab922023eec51879ce067831b64d18c1bd30fd9339')

package() {
  # The release .deb already has the right layout: /usr/bin, the .desktop
  # entry and hicolor icons.
  bsdtar -xf "$srcdir/data.tar.gz" -C "$pkgdir"
  install -Dm644 "$srcdir/LICENSE-$pkgver" \
    "$pkgdir/usr/share/licenses/$pkgname/LICENSE"
}
